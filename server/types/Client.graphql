type Client {
  id: ID!
  name: String
  age: Int
}

extend type Query {
  clients: [Client!]!
  client(id: ID!): Client
}

extend type Mutation {
  addClient(name: String!, age: Int!, id: ID!): Client
}
