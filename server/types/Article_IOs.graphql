# INPUTS

input Body {
  authorID: ID
  title: String!
  body: String!
}

input Changes {
  title: String
  body: String
}

input ArticleQueryInput {
  keywords: String
  authorID: String
  createdAt: Date
}

#RESPONSES

type newArticleResponse {
  success: Boolean
  articleID: ID
  errors: [ErrorFormat]
}

type editArticleResponse {
  success: Boolean
  errors: [ErrorFormat]
}

type removeArticleResponse {
  success: Boolean
  errors: [ErrorFormat]
}
