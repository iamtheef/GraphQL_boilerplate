overwrite: true
schema: "./types/*.graphql"
documents: null
generates:
  ./schema/schema.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
