overwrite: true
schema: "./types/*.graphql"
documents: null
generates:
  ./generated/schema.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
